<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="co.huby.prj.memberVideo.mapper.MemberVideoMap">
	<insert id="memberVideoInsert" parameterType="map">
		INSERT INTO VIDEO
		(VIDEO_ID,MEMBER_ID, VIDEO_IMG,HASHTAG,
		VIDEO_LOCATION)
		VALUES(VIDEO_SEQ.NEXTVAL,#{member_id},
		#{video_img}, (select code_id from code where code_name = #{hashtag}),
		#{video_location})
	</insert>
</mapper>